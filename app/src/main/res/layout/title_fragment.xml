<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/titleImage"
        android:layout_width="@dimen/titleImageWidth"
        android:layout_height="@dimen/titleImageHeight"
        android:src="@mipmap/quiz_time"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.3"
        android:contentDescription="@string/titleImageContentDesc"
    />

    <Button
        android:id="@+id/playButton"
        android:layout_width="@dimen/playButtonWidth"
        android:layout_height="@dimen/playButtonHeight"
        android:text="@string/playButton"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.7"
        style="@style/playButton"
        />

</androidx.constraintlayout.widget.ConstraintLayout>